### 简介
---
此虚拟机基于Pcode设计，仿brainFuck图灵机设计，纸带仅单向。

地址从0到正无穷，每个格子可存储一位int类型十进制数（为什么不用二进制主要是虚拟机指令可读性差）。

### 指令设计
---
push 往当前指针所在内存空间压入一个十进制操作数

add 往当前指针所在内存空间值增加操作数

sub 往当前指针所在内存空间值减去操作数（真的有sub的必要吗？add -1 不就行了吗？）

left 指针左移

right 指针右移

start/end 成对出现，当指针所在空间值为0时，从start跳到end处，当指针值不为零时，从end处

print 打印当前指针空间值
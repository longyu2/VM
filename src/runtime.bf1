// 真正试图自举了
// 代码将被写入本内存纸带中

import ./src/tool.bf1


// 迄今为止，依旧使用文件体系来输入指令，数据和指令并不是同时处理的。
// 所以，需要将程序指令输入内存中，来让程序读取，这才是我的目标



// 实现Switch,最外层，判断非0，如果0，说明程序错误不进入循环



// 对代码进行Switch以后，就可以开始执行了

// 需要一个循环，不停地右移，然后执行,某种switch,直到代码段结束


right 20
 
// 取指令，复制到10
moveL10()
left 10


     
      







